using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerDashScript : MonoBehaviour{

/*public float activeMoveSpeed;
public float dashSpeed;
public float dashLength = 5f, dashcooldown = 1f; //Remove if no cooldown
private float dashCounter;
private float dashCDCounter; //Remove if no cooldown

void Start()
{
    activeMoveSpeed = movSpeed; //Replace "moveSpeed" with player movement speed in script 
}

void Update()
{
    Player.velocity = moveInput * activeMoveSpeed; //Replace "Player" with actual player name in project

    if (Input.GetKeyDown(KeyCode.Space)) //Replace with mobile UI buttons 
    {
        if (dashCDCounter <=0 && dashCounter <=0) //Perform a dash
        {
            activeMoveSpeed = dashSpeed;
            dashCounter = dashLength;
        }
    }
    if (dashCounter <=0) //if dash reaches 0 then reset back to original speed
    {
        dashCounter -= Time.deltaTime; //Updates every frame (Frame base, could be an issue)
        if(dashCounter <=0)
        {
            activeMoveSpeed = moveSpeed; //Player movespeed
            dashCDCounter = dashCooldown;
        }
    }
    if (dashCDCounter > 0) //Just counts down until dash is available again
    {
        dashCDCounter -= Time.deltaTime;
    }
} //Note: Looking at mobile controller tutorials, all controller should be stated so that the Mobile Controller Package
            //can acess these functions. I don't know that much but that is what I understand
*/
}
